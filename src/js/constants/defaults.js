'use strict';

module.exports = Object.freeze({
	apiConfig: {
		apiAuthorityPart: 'api.zotero.org'
	},
	preferences: {
		columns: [
			{ field: 'title', fraction: 0.5 },
			{ field: 'creator', fraction: 0.3 },
			{ field: 'date', fraction: 0.2 },
		]
	}
});
